# Summarize BSS model runtime

Summarize BSS model runtime

## Usage

``` r
get_bss_runtime(stan_fit, output_file = NULL)
```

## Arguments

- stan_fit:

  A 'stanfit' class object

- output_file:

  Directory to save output as text file
