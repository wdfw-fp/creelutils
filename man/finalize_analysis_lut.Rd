% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/finalize_analysis_lut.R
\name{finalize_analysis_lut}
\alias{finalize_analysis_lut}
\title{Finalize the analysis look-up table for export}
\usage{
finalize_analysis_lut(analysis_lut, resolved_params, conn = NULL)
}
\arguments{
\item{analysis_lut}{lookup table created during the model estimation
process which stores a session-specific analysis_id key and metadata
about the analysis.}

\item{resolved_params}{user-input parameters defined during the model estimation
process.}

\item{conn}{a valid \code{DBI} connection, or NULL for local export.
@seealso \code{\link[=connect_creel_db]{connect_creel_db()}}}
}
\value{
The finalized analysis_lut, ready for \code{write_lut()} (database)
or local archiving.
}
\description{
Completes the session \code{analysis_lut} created by \code{generate_analysis_lut()}
immediately prior to export. Adds ETL-time metadata (r_session_json,
data_grade), and, when a database connection is supplied, resolves
project/fishery names to database UUIDs and validates the final column
set against the model_analysis_lut schema.
}
\details{
With \code{con = NULL} (local export) the lut retains \code{project_name} and
\code{fishery_name}; id resolution is deterministic and occurs whenever the
session is later exported to the database.
}
\seealso{
Other ETL:
\code{\link[=export_estimates]{export_estimates()}},
\code{\link[=json_conversion]{json_conversion()}},
\code{\link[=map_data_grade]{map_data_grade()}},
\code{\link[=prep_export]{prep_export()}},
\code{\link[=process_estimates_bss]{process_estimates_bss()}},
\code{\link[=process_estimates_pe]{process_estimates_pe()}},
\code{\link[=transform_estimates]{transform_estimates()}},
\code{\link[=update_analysis_comment]{update_analysis_comment()}}
}
\concept{ETL}
