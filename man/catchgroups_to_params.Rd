% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catchgroups_to_params.R
\name{catchgroups_to_params}
\alias{catchgroups_to_params}
\title{Format catch groups as YAML param strings}
\usage{
catchgroups_to_params(data, print = FALSE)
}
\arguments{
\item{data}{A tibble with \code{species}, \code{life_stage}, \code{fin_mark}, and \code{fate}
columns, typically the output of \code{\link[=fishery_catchgroups_obs]{fishery_catchgroups_obs()}} or
\code{\link[=fishery_catchgroups]{fishery_catchgroups()}}.}

\item{print}{Logical. If \code{TRUE}, prints \code{param_string} values to the console
separated by \verb{,\\n} (ready to paste into a YAML param) and returns the
tibble invisibly. Default \code{FALSE}.}
}
\value{
The input tibble with an appended \code{param_string} character column.
Returned invisibly when \code{print = TRUE}.
}
\description{
Adds a \code{param_string} column to a catch groups tibble, formatting each row
as a \code{c(species = '...', life_stage = '...', fin_mark = '...', fate = '...')}
string suitable for pasting into the \code{est_catch_groups} YAML param of a
CreelEstimates script.

Composite catch groups (e.g. \code{Adult|Jack}, \code{AD|UM|UNK}) are preserved
literally — one row in equals one \code{param_string} out.

Typical workflow: pipe the output of \code{\link[=fishery_catchgroups_obs]{fishery_catchgroups_obs()}} through
this function with \code{print = TRUE} to emit the comma-separated rows to the
console, then paste between the \verb{rbind(} and \verb{))} of the script's
\code{est_catch_groups} YAML param.
}
\examples{
\dontrun{
con <- connect_creel_db()
dwg <- fetch_dwg("Skagit fall salmon 2025")

# Print to console for copy-paste into est_catch_groups
fishery_catchgroups_obs(con, dwg) |>
  catchgroups_to_params(print = TRUE)
}
}
