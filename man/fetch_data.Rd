% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_data.R
\name{fetch_data}
\alias{fetch_data}
\title{Fetch raw datasets from the internal database (alternative to fetch_dwg)}
\usage{
fetch_data(
  conn = NULL,
  fishery_name,
  tables = c("effort", "ll", "interview", "catch", "closures", "fishery_manager")
)
}
\arguments{
\item{conn}{A valid database connection from \code{\link[=connect_creel_db]{connect_creel_db()}}. If \code{NULL}
(default), a connection is opened internally and closed on exit.}

\item{fishery_name}{Character string. The exact fishery name to filter on.}

\item{tables}{Character vector of data components to retrieve. Defaults to
all six: \code{"effort"}, \code{"ll"}, \code{"interview"}, \code{"catch"}, \code{"closures"},
\code{"fishery_manager"}. Subset as needed, e.g. \code{tables = c("catch", "interview")}.}
}
\value{
A named list of tibbles, ordered to match \code{\link[=fetch_dwg]{fetch_dwg()}} output:
\verb{$effort}, \verb{$ll}, \verb{$interview}, \verb{$catch}, \verb{$closures}, \verb{$fishery_manager}.
Only requested tables are included.
}
\description{
Queries creel data views from the WDFW PostgreSQL database for a
single fishery. Returns a named list with the same structure as \code{\link[=fetch_dwg]{fetch_dwg()}},
making it a drop-in replacement for sourcing data internally instead of
through the public data portal (data.wa.gov).
}
\details{
The \code{ll} (i.e., lat/long water body coordinates) table is derived by filtering
\code{water_body_lut} to water bodies present in the effort data. If \code{"ll"} is
requested without \code{"effort"}, the effort view is queried internally to
resolve water body names but is not included in the returned list.
}
\examples{
\dontrun{
# All tables for a fishery
data <- fetch_data(fishery_name = "Skagit fall salmon 2025")

# Just catch and interview
data <- fetch_data(fishery_name = "Skagit fall salmon 2025",
                   tables = c("catch", "interview"))

# With an existing connection
con <- connect_creel_db()
data <- fetch_data(conn = con, fishery_name = "Skagit fall salmon 2025")
DBI::dbDisconnect(con)
}
}
\seealso{
Other internal_data: 
\code{\link{connect_creel_db}()},
\code{\link{fetch_db_table}()},
\code{\link{fishery_catchgroups}()},
\code{\link{fishery_catchgroups_obs}()}
}
\concept{internal_data}
