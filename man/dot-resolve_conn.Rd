% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect_creel_db.R
\name{.resolve_conn}
\alias{.resolve_conn}
\title{Resolve a database connection argument}
\usage{
.resolve_conn(conn = NULL, arg = "conn", call = parent.frame())
}
\arguments{
\item{conn}{A DBI connection, or \code{NULL} to open one lazily.}

\item{arg}{Name of the argument being resolved, for error messages.}

\item{call}{Caller environment; also where the lazy connection's
disconnect handler is registered.}
}
\value{
A valid DBI connection.
}
\description{
Centralizes connection handling for functions taking a \code{conn} argument.
\code{NULL} opens a lazy connection that is closed when the calling function
exits. An invalid (closed or stale) connection is a loud error, never a
silent reconnect. Prior to v0.3.0, a silent reconnect defaulted as
\code{connect_creel_db(db_env = "prod")} and could reroute a stale \code{test}
connection inadvertently to \code{prod}.
}
\keyword{internal}
