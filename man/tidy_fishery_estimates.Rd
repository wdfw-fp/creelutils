% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy_fishery_estimates.R
\name{tidy_fishery_estimates}
\alias{tidy_fishery_estimates}
\title{Convert fishery estimates from long to wide (tidy) format}
\usage{
tidy_fishery_estimates(estimates_df)
}
\arguments{
\item{estimates_df}{A dataframe of estimates, typically from \code{get_fishery_estimates()$estimates}}
}
\value{
A list with two tibbles:
\itemize{
\item \verb{$catch}: Wide format catch estimates (one row per analysis_id + catch_group)
\item \verb{$effort}: Wide format effort estimates (one row per analysis_id)
}
}
\description{
Separates estimates into catch and effort tables, then pivots each
so that each unique combination of analysis_id and catch_group has one row with
all estimate types as separate columns. Estimate types are prefixed with model
type (PE or BSS) to distinguish the source.

Available estimate types in data:
\itemize{
\item estimate_sum
\item totaldaysopen
\item totalobs
\item mean
\item standard_error_mean
\item standard_deviation
\item quantile_lower_2_5
\item quantile_lower_25
\item quantile_median_50
\item quantile_upper_75
\item quantile_upper_97_5
\item n_eff
\item R_hat
\item n_div
}
}
\examples{
\dontrun{
# Get estimates
nisqually_est <- get_fishery_estimates(
  fishery_names = "Nisqually salmon",
  years = 2022
)

# Convert to tidy format
tidy_est <- tidy_fishery_estimates(nisqually_est$estimates)

# Access catch and effort separately
catch_data <- tidy_est$catch
effort_data <- tidy_est$effort
}
}
\concept{data_formatting}
